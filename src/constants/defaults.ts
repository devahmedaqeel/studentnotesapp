import { AppSettings } from '../types/common';

export const DEFAULT_APP_SETTINGS: AppSettings = {
  themeMode: 'system',
  defaultScanQuality: 0.8,
  defaultPdfQuality: 0.8,
};

export const STORAGE_DIR_NAME = 'app-storage';
